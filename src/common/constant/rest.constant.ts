const BASE_URL = 'https://jsonplaceholder.typicode.com';

const RestConstant = {
  githubApi: {
    getAllUsers: `${BASE_URL}/users`,
    getUser: `${BASE_URL}/users`,
    getAllPosts: `${BASE_URL}/posts`,
  },
};

export default RestConstant;
